// Platform-wide LLM defaults used when a tenant hasn't picked its own
// provider/model (Option B — shared platform key).

export const DEFAULT_PROVIDER: 'openai' | 'anthropic' = 'openai';
export const DEFAULT_MODEL = 'gpt-4o-mini';
