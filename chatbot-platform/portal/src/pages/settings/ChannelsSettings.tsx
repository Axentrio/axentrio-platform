/**
 * Channels Settings
 * Thin wrapper around SocialChannelsContent. Also rendered in /ai?tab=social.
 */

import { SocialChannelsContent } from '@/components/channels/SocialChannelsContent';

export default function ChannelsSettings() {
  return <SocialChannelsContent />;
}
